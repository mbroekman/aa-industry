[build-system]
build-backend = "poetry.core.masonry.api"
requires = [ "poetry-core" ]

[tool.poetry]
name = "ai-forecasting"
version = "0.1.0"
description = "AI Demand Forecasting Service"
authors = [ "Admin <admin@example.com>" ]
readme = "README.md"
packages = [ { include = "ai_forecasting" } ]
dependencies.python = "^3.11"
dependencies.fastapi = "^0.110.0"
dependencies.joblib = "^1.3.2"
dependencies.lightgbm = "^4.3.0"
dependencies.pandas = "^2.2.1"
dependencies.scikit-learn = "^1.4.1"
dependencies.sqlalchemy = "^2.0.28"
dependencies.uvicorn = { extras = [ "standard" ], version = "^0.27.1" }
